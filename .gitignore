# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.production
.env.local
.env.push

# Catch-all for non-production / non-app material kept locally but out of the
# repo: Wix site code, competitor research, scratch notes, etc.
ignore/

# Local Claude Code orientation — not shipped with the open-source package
CLAUDE.md
# Throwaway agent worktrees: full repo copies (14M+ and growing), never content.
.claude/

# Wrangler's local build/deploy scratch state.
.wrangler/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
backups/
